% sections/appendix.tex

\section{Alpha-Sweep Risk--Coverage Tradeoff}
\label{app:alpha}

\begin{table*}[t]
\centering
\small
\caption{Alpha-sweep risk-coverage tradeoff (logistic fusion, 5 seeds, \emph{pooled} across seeds). Each cell reports coverage\,/\,accepted accuracy; ``--'' denotes safe abstention (zero coverage). Coverage is \emph{positively correlated} with the risk budget $\alpha$ but not strictly monotone: because the accepted prefix is chosen data-dependently on each split, relaxing $\alpha$ occasionally selects a different threshold that lowers pooled coverage. We report the trend as-is rather than enforcing monotonicity.}
\label{tab:alpha}
\begin{tabular}{llcccccc}
\toprule
Model & Dataset & $\alpha{=}0.05$ & $\alpha{=}0.10$ & $\alpha{=}0.15$ & $\alpha{=}0.20$ & $\alpha{=}0.25$ & $\alpha{=}0.30$ \\
\midrule
\multirow{4}{*}{DeepSeek-V4} & JudgeBench & --/-- & .03/.96 & .03/.93 & .03/.93 & .24/.78 & .46/.71 \\
 & TL;DR & --/-- & --/-- & .09/.84 & .13/.85 & .27/.79 & .67/.73 \\
 & RewardBench & .82/.97 & 1.00/.93 & 1.00/.93 & 1.00/.93 & 1.00/.93 & 1.00/.93 \\
 & LMArena & --/-- & --/-- & .07/.89 & .05/.90 & .63/.78 & .97/.73 \\
\midrule
\multirow{4}{*}{GPT-5.5} & JudgeBench & --/-- & .25/.94 & .32/.90 & .39/.88 & .42/.82 & .44/.79 \\
 & TL;DR & --/-- & .02/.96 & .06/.98 & .18/.87 & .59/.79 & .73/.74 \\
 & RewardBench & .73/.96 & .79/.92 & .82/.88 & .86/.84 & .91/.79 & .97/.74 \\
 & LMArena & --/-- & --/-- & .05/.91 & .09/.88 & .39/.79 & .63/.75 \\
\bottomrule
\end{tabular}
\end{table*}

\section{Reproducibility Details}
\label{app:repro}

\paragraph{Judge model configurations.}
GPT-5.5 was queried through an OpenAI-compatible endpoint at its default reasoning effort; DeepSeek-V4 was queried in non-thinking (\texttt{deepseek-chat}) mode because the reasoning \texttt{v4-pro} mode is too token-intensive for six-call-per-item feature collection. The controlled capability ladder uses \texttt{Qwen/Qwen2.5-1.5B-Instruct}, \texttt{Qwen/Qwen2.5-7B-Instruct}, and \texttt{Qwen/Qwen2.5-14B-Instruct} run locally in fp16 on a single 24\,GB GPU, with identical prompts, chat template, and decoding settings so that only parameter count varies. All deterministic verdicts use temperature $0$; self-consistency samples use temperature $0.7$ with \texttt{max\_new\_tokens}$=80$. Base confidence is the model's verbalized confidence, since token log-probabilities are not exposed by all endpoints. We pin and release the exact API model snapshot identifiers and dates, the local model commit hashes, all prompt templates, the six-call collection code, per-seed split indices, and the fitted calibrators; the AAAI reproducibility checklist is completed in the supplementary archive.

\paragraph{Tie and invalid handling.}
An output that cannot be parsed as $A$/$B$ is retried once. If it is still unparseable we record it as an explicit \texttt{invalid} state rather than silently mapping it to the base verdict: an \texttt{invalid} perturbation call counts as a \emph{disagreement} with the base verdict, so it \emph{lowers} the corresponding stability signal and can only push an item toward abstention. We deliberately avoid the tempting shortcut of mapping invalid$\to$base verdict, because that would artificially inflate measured stability (an unparseable output would be counted as agreement). Invalid rates are below 3\% for every judge--benchmark pair. For the preference benchmarks (TL;DR, LMArena) genuine ties in the human label are dropped before evaluation; JudgeBench and RewardBench carry objective correct/incorrect labels and contain no ties.

\paragraph{Rubric variants (semantic-equivalence caveat).}
The $K{=}3$ rubric paraphrases used for the rubric-stability signal are reproduced verbatim in the released prompt file. They were written to hold the evaluation target (which response is better) fixed while varying wording. Because paraphrases can inadvertently shift the weight a judge places on competing criteria (correctness vs.\ helpfulness vs.\ style), we ran a negative-control condition with \emph{intentionally non-equivalent} rubrics (one emphasizing brevity, one thoroughness, one formality) on Qwen2.5-1.5B/JudgeBench. The equivalent set produced a flip rate of 31.0\% (189/610) while the non-equivalent set produced 16.7\% (102/610)---surprisingly, the non-equivalent set was \emph{more} stable, not less. We attribute this to the near-random nature of the 1.5B judge: it latches onto surface features regardless of rubric content, so changing the criterion does not increase its instability. This finding reinforces the competence-gated asymmetry (Section~\ref{sec:exp:stablewrong}): below the competence threshold, rubric perturbation does not cleanly separate wording from criterion sensitivity, and the stability signal should not be trusted. A human check of pairwise rubric equivalence and a formal criterion-vs-wording decomposition on mid-capability judges remain future work.

\paragraph{Per-seed risk reporting.}
For every model--dataset--$\alpha$ setting we release, per seed: the accepted count, coverage, realized test error $\hat R_{\text{test}}$, the Clopper--Pearson upper bound on the calibration split, and whether $\hat R_{\text{test}}>\alpha$. To characterize variance rather than only central tendency, we resample the 40/30/30 split over many random seeds---up to several hundred paired splits per setting where feasible---and report coverage and selective risk as mean$\pm$std across these splits alongside the pooled point estimates used in the main tables (pooling errors and accepts, rather than averaging per-seed ratios, so that zero-coverage seeds neither dominate nor are silently dropped). Aggregate seed robustness is high (coverage coefficient of variation $\approx2.4\%$ at $\alpha{=}0.15$). We also report the empirical risk-violation frequency $\Pr_{\text{seed}}(\hat R_{\text{test}}>\alpha)$ as our direct check on risk control; because the acceptance threshold is chosen data-dependently, this empirical frequency---not a formal family-wise guarantee---is the quantity we ask readers to judge, and occasional violations at very low coverage (where only a handful of items are accepted) are expected and reported transparently.

\section{Exploratory: Cost-Aware Cascade}
\label{app:cascade}

This section uses the \textbf{same strict 40/30/30 split as the main experiments} and calibrates each tier \emph{conditionally} on the calibration items that actually reach it, with error budget $\delta_t=\delta/T$ (Section~\ref{sec:method:cascade}). We still label it exploratory because it uses a single labeled pool per dataset and we have not stress-tested the conditional calibration under distribution shift.

We evaluate a three-tier cascade (Qwen-1.5B $\to$ DeepSeek-V4 $\to$ GPT-5.5), averaging over 10 seeds (Table~\ref{tab:cascade}). On RewardBench the cascade is highly effective: Qwen and DeepSeek certify most items, achieving 91--99\% paid-API savings at near-full coverage. On the subjective benchmarks (TL;DR, LMArena) and JudgeBench, the cheap tiers cannot certify the risk bound at strict $\alpha$, so items escalate to GPT-5.5 and savings are near zero or negative; savings become positive only at loose $\alpha{=}0.30$ where DeepSeek begins to certify. Savings are \emph{paid-API-token cost avoided on the accepted subset} relative to a single-call GPT-5.5 judge, and exclude local GPU time, throughput, and latency.

\begin{table}[t]
\centering
\small
\setlength{\tabcolsep}{4pt}
\caption{Cost-aware cascade (strict 40/30/30 split, conditional per-tier calibration, $\delta_t{=}\delta/T$, 10 seeds). Cov/Acc on held-out test; Save is paid-API-token cost avoided vs.\ single-call GPT-5.5.}
\label{tab:cascade}
\begin{tabular}{llrrr}
\toprule
Dataset & $\alpha$ & Cov.\ & Acc.\ & Save \\
\midrule
\multirow{2}{*}{JudgeBench} & 0.15 & 0.258 & 0.933 & $-$11\% \\
 & 0.30 & 0.538 & 0.799 & 11\% \\
\midrule
\multirow{2}{*}{TL;DR} & 0.15 & 0.051 & 0.935 & $-$11\% \\
 & 0.30 & 0.552 & 0.806 & 16\% \\
\midrule
\multirow{2}{*}{RewardBench} & 0.15 & 0.998 & 0.914 & 91\% \\
 & 0.30 & 0.998 & 0.780 & 99\% \\
\midrule
\multirow{2}{*}{LMArena} & 0.15 & 0.045 & 0.869 & $-$7\% \\
 & 0.30 & 0.597 & 0.794 & 46\% \\
\bottomrule
\end{tabular}
\end{table}

\section{Exploratory: Cross-Dataset Transfer}
\label{app:transfer}

We use the strict three-way split: the calibrator is fit on the \emph{source} dataset (train+cal) and evaluated on a held-out \emph{target} test split, averaged over 10 seeds (Table~\ref{tab:transfer}, GPT-5.5, $\alpha{=}0.15$). The pattern is clear: calibrators trained on \emph{subjective} benchmarks (LMArena, TL;DR) transfer with very high precision but low coverage---an LMArena-trained calibrator accepts only 3.9\% of JudgeBench but at 100\% accuracy---while calibrators trained on \emph{objective} benchmarks (JudgeBench, RewardBench) transfer with higher coverage but lower accuracy (65.6--69.8\%). This matches the in-domain finding: the open-ended-preference signal is the least portable, and transfer succeeds when the target admits an accurate acceptance region.

\begin{table}[t]
\centering
\small
\setlength{\tabcolsep}{4pt}
\caption{Cross-dataset transfer for GPT-5.5 ($\alpha{=}0.15$, strict 3-way split, source train+cal $\to$ target held-out test, 10 seeds). Subjective sources (LMArena, TL;DR) yield precise but low-coverage transfer; objective sources (JudgeBench, RewardBench) yield higher coverage at lower accuracy.}
\label{tab:transfer}
\begin{tabular}{lrr}
\toprule
Source $\to$ Target & Cov.\ & Acc.\ \\
\midrule
TL;DR $\to$ JudgeBench & 0.107 & 0.995 \\
TL;DR $\to$ RewardBench & 0.536 & 0.986 \\
TL;DR $\to$ LMArena & 0.174 & 0.839 \\
JudgeBench $\to$ TL;DR & 0.539 & 0.756 \\
JudgeBench $\to$ RewardBench & 0.682 & 0.958 \\
JudgeBench $\to$ LMArena & 0.555 & 0.745 \\
RewardBench $\to$ JudgeBench & 0.558 & 0.656 \\
RewardBench $\to$ TL;DR & 0.789 & 0.697 \\
RewardBench $\to$ LMArena & 0.769 & 0.698 \\
LMArena $\to$ JudgeBench & 0.039 & 1.000 \\
LMArena $\to$ TL;DR & 0.006 & 1.000 \\
LMArena $\to$ RewardBench & 0.228 & 0.998 \\
\bottomrule
\end{tabular}
\end{table}

\section{Risk--Coverage Curves}
\label{app:rcfig}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/risk_coverage.pdf}
\caption{Held-out risk--coverage curves (logistic fusion). Lower is better; the dashed line marks the $\alpha{=}0.15$ risk budget. GPT-5.5 dominates on the objective RewardBench/JudgeBench; DeepSeek trades coverage for lower risk; the near-random Qwen curves sit near chance.}
\label{fig:rc}
\end{figure}

\section{Signal-Level Accuracy Gaps}
\label{app:signals}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/signal_gap.pdf}
\caption{Judge accuracy on rubric-stable vs.\ rubric-unstable subsets. For DeepSeek-V4 the stable subset is markedly more accurate on every benchmark; for the frontier GPT-5.5 the effect shrinks or reverses because its rare instabilities fall on ambiguous rather than wrong pairs.}
\label{fig:gap}
\end{figure}

\begin{table}[t]
\centering
\small
\setlength{\tabcolsep}{4pt}
\caption{Signal-level accuracy: stable vs.\ unstable subsets, for judge--benchmark pairs with $\geq$60\% raw accuracy. DeepSeek-V4 shows consistent positive gaps; GPT-5.5's rare instabilities do not track error (RewardBench).}
\label{tab:signals}
\begin{tabular}{lllrrr}
\toprule
Signal & Model & Dataset & Stable & Unstable & Gap \\
\midrule
\multirow{5}{*}{Rubric} & DeepSeek & JudgeBench & 0.689 & 0.648 & +0.041 \\
 & DeepSeek & TL;DR & 0.708 & 0.555 & +0.153 \\
 & DeepSeek & RewardBench & 0.937 & 0.664 & +0.274 \\
 & DeepSeek & LMArena & 0.749 & 0.478 & +0.271 \\
 & GPT-5.5 & RewardBench & 0.709 & 0.762 & $-$0.053 \\
\midrule
\multirow{5}{*}{Position} & DeepSeek & JudgeBench & 0.728 & 0.575 & +0.153 \\
 & DeepSeek & TL;DR & 0.746 & 0.518 & +0.228 \\
 & DeepSeek & RewardBench & 0.929 & 0.867 & +0.062 \\
 & DeepSeek & LMArena & 0.772 & 0.528 & +0.244 \\
 & GPT-5.5 & RewardBench & 0.708 & 0.850 & $-$0.142 \\
\bottomrule
\end{tabular}
\end{table}

\section{Ensemble Baselines and Feature-Group Ablation}
\label{app:ensemble}

\begin{table}[t]
\centering
\small
\setlength{\tabcolsep}{3.2pt}
\caption{Ensemble baselines vs.\ CARE (held-out AUROC, 20 seeds). CARE wins or ties in 9/12 pairs; weighted vote is competitive for GPT-5.5 (uses confidence implicitly) but lacks risk control.}
\label{tab:ensemble}
\begin{tabular}{llrrrr}
\toprule
Model & Data & \textbf{CARE} & SCOPE & MajVote & WtdVote \\
\midrule
\multirow{4}{*}{DeepSeek} & JB & \textbf{0.603} & 0.561 & 0.585 & 0.581 \\
 & TL;DR & \textbf{0.675} & 0.616 & 0.609 & 0.618 \\
 & RB & \textbf{0.831} & 0.606 & 0.675 & 0.810 \\
 & Arena & \textbf{0.678} & 0.615 & 0.612 & 0.616 \\
\midrule
\multirow{4}{*}{GPT-5.5} & JB & \textbf{0.986} & 0.177 & 0.974 & 0.985 \\
 & TL;DR & 0.862 & 0.315 & 0.808 & \textbf{0.864} \\
 & RB & 0.990 & 0.268 & 0.959 & \textbf{0.991} \\
 & Arena & \textbf{0.845} & 0.320 & 0.804 & 0.845 \\
\midrule
\multirow{4}{*}{Qwen} & JB & 0.523 & \textbf{0.534} & 0.505 & 0.509 \\
 & TL;DR & \textbf{0.583} & 0.570 & 0.544 & 0.494 \\
 & RB & \textbf{0.713} & 0.425 & 0.538 & 0.620 \\
 & Arena & \textbf{0.578} & 0.563 & 0.559 & 0.494 \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[t]
\centering
\small
\setlength{\tabcolsep}{3.5pt}
\caption{Feature-group ablation (held-out AUROC, 20 seeds). ``FULL'' = all 14 features; each ablation uses only that group. Confidence dominates for GPT-5.5; protocol for Qwen; length is near-chance everywhere.}
\label{tab:featgroup}
\begin{tabular}{llrrrr}
\toprule
Model & Data & \textbf{FULL} & protocol & conf & length \\
\midrule
\multirow{4}{*}{DeepSeek} & JB & \textbf{0.603} & 0.587 & 0.623 & 0.509 \\
 & TL;DR & 0.675 & 0.660 & \textbf{0.683} & 0.548 \\
 & RB & \textbf{0.831} & 0.699 & 0.828 & 0.622 \\
 & Arena & \textbf{0.678} & 0.654 & 0.680 & 0.531 \\
\midrule
\multirow{4}{*}{GPT-5.5} & JB & 0.986 & 0.971 & \textbf{0.988} & 0.531 \\
 & TL;DR & 0.862 & 0.826 & \textbf{0.865} & 0.517 \\
 & RB & 0.990 & 0.971 & \textbf{0.991} & 0.502 \\
 & Arena & 0.845 & 0.815 & \textbf{0.849} & 0.503 \\
\midrule
\multirow{4}{*}{Qwen} & JB & 0.523 & \textbf{0.535} & 0.494 & 0.497 \\
 & TL;DR & \textbf{0.583} & 0.580 & 0.581 & 0.536 \\
 & RB & \textbf{0.713} & 0.659 & 0.631 & 0.609 \\
 & Arena & 0.578 & 0.579 & \textbf{0.587} & 0.535 \\
\bottomrule
\end{tabular}
\end{table}
