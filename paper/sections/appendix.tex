% sections/appendix.tex

\section{CARE-Judge Algorithm}
\label{app:algo}

\begin{algorithm}[h]
\caption{CARE-Judge selective evaluation}
\label{alg:care}
\begin{algorithmic}[1]
\STATE \textbf{Input:} labeled items $D$; risk $\alpha$, level $\delta$; min-accept $m$
\STATE Split $D$ into disjoint $D_{\text{train}}, D_{\text{cal}}, D_{\text{test}}$ (40/30/30)
\STATE Fit calibrator $\hat p$ on $\{(\boldsymbol\phi(x), \mathbb{1}[\hat f(x){=}y])\}_{x\in D_{\text{train}}}$
\STATE Sort $D_{\text{cal}}$ by $\hat p$ descending: $c_{(1)}\ge\cdots\ge c_{(n)}$
\STATE $\hat\lambda \gets \infty$ \COMMENT{accept nothing by default}
\FOR{$i = m$ \TO $n$}
  \STATE $e_i \gets \#\{j\le i: \hat f(x_{(j)})\ne y_{(j)}\}$ \COMMENT{errors in top-$i$ prefix}
  \IF{$\text{CP}_{\text{upper}}(e_i, i, \delta) \le \alpha$}
    \STATE $\hat\lambda \gets \hat p(x_{(i)})$ \COMMENT{largest certified prefix so far}
  \ENDIF
\ENDFOR
\STATE \textbf{Output:} on $D_{\text{test}}$, accept $x$ iff $\hat p(x)\ge\hat\lambda$, else abstain
\STATE \textbf{Report:} empirical violation rate $\Pr_{\text{seed}}(\hat R_{\text{test}}>\alpha)$ over splits
\end{algorithmic}
\end{algorithm}

\section{Alpha-Sweep Risk--Coverage Tradeoff}
\label{app:alpha}
Table~\ref{tab:alpha} traces how coverage and accepted accuracy vary with the risk budget $\alpha$ for the two API judges, extending the single $\alpha{=}0.15$ operating point of Table~\ref{tab:main} to the full $0.05$--$0.30$ range.

\begin{table}[t]
\centering
\small
\caption{Alpha-sweep risk-coverage tradeoff (logistic fusion, 20 seeds \emph{pooled} across seeds, recomputed from the per-item feature traces at each of the six $\alpha$ levels, consistent with the $\alpha{=}0.15$ column of Table~\ref{tab:main}). We show the two API judges (DeepSeek-V4, GPT-5.5), which have non-trivial coverage across this range; the near-chance ladder judges abstain at zero coverage across most of it (Table~\ref{tab:main}) and are omitted rather than shown as all-``--'' rows. Each cell reports coverage\,/\,accepted accuracy; ``--'' denotes safe abstention (zero coverage) and ``$<$.01'' a positive coverage below $0.005$. Coverage rises with the risk budget $\alpha$ but is not strictly monotone: because the accepted prefix is chosen data-dependently on each split, relaxing $\alpha$ occasionally selects a different threshold that shifts pooled coverage.}
\label{tab:alpha}
\begin{tabular}{llcccccc}
\toprule
Model & Dataset & $\alpha{=}0.05$ & $\alpha{=}0.10$ & $\alpha{=}0.15$ & $\alpha{=}0.20$ & $\alpha{=}0.25$ & $\alpha{=}0.30$ \\
\midrule
\multirow{4}{*}{DeepSeek-V4} & JudgeBench & --/-- & .01/.96 & .01/.92 & .04/.88 & .15/.76 & .51/.72 \\
 & TL;DR & --/-- & $<$.01/.82 & .08/.83 & .32/.81 & .59/.77 & .82/.72 \\
 & RewardBench & .78/.96 & 1.00/.93 & 1.00/.93 & 1.00/.93 & 1.00/.93 & 1.00/.93 \\
 & LMArena & --/-- & .01/.89 & .08/.87 & .45/.82 & .77/.77 & .96/.73 \\
\midrule
\multirow{4}{*}{GPT-5.5} & JudgeBench & .52/.97 & .95/.93 & .99/.92 & .99/.92 & .99/.92 & .99/.92 \\
 & TL;DR & .03/.92 & .08/.89 & .22/.86 & .50/.82 & .82/.77 & .99/.73 \\
 & RewardBench & .99/.96 & 1.00/.96 & 1.00/.96 & 1.00/.96 & 1.00/.96 & 1.00/.96 \\
 & LMArena & $<$.01/.90 & .02/.91 & .13/.85 & .34/.81 & .59/.77 & .88/.73 \\
\bottomrule
\end{tabular}
\end{table}

\section{Reproducibility Details}
\label{app:repro}

\paragraph{Judge model configurations.}
GPT-5.5 was queried through an OpenAI-compatible endpoint at its default reasoning effort; DeepSeek-V4 was queried in non-thinking (\texttt{deepseek-chat}) mode because the reasoning \texttt{v4-pro} mode is too token-intensive for six-call-per-item feature collection. The controlled capability ladder uses \texttt{Qwen/Qwen2.5-1.5B-Instruct}, \texttt{Qwen/Qwen2.5-7B-Instruct}, and \texttt{Qwen/Qwen2.5-14B-Instruct} run locally in fp16 on a single 24\,GB GPU, with identical prompts, chat template, and decoding settings so that only parameter count varies. All deterministic verdicts use temperature $0$; self-consistency samples use temperature $0.7$ with \texttt{max\_new\_tokens}$=80$. Base confidence is the model's verbalized confidence, since token log-probabilities are not exposed by all endpoints. We pin and release the exact API model snapshot identifiers and dates, the local model commit hashes, all prompt templates, the six-call collection code, per-seed split indices, and the fitted calibrators; the AAAI reproducibility checklist is completed in the supplementary archive.

\paragraph{Compute budget.}
All local judges (the Qwen2.5 ladder) were run on a single 32\,GB consumer GPU (RTX 5090). The 1.5B judge collects features at $\approx$1{,}500 items/hour, the 7B at $\approx$450/hour, and the 14B at $\approx$200/hour; total local feature collection for the ladder was under 40 GPU-hours. Each item consumes exactly six judge calls, so feature collection for the API judges required $6\times6{,}620\approx40{,}000$ calls per API model (DeepSeek-V4 and GPT-5.5), plus $2\times898$ additional calls for the DeepSeek negative-control. The calibrator fit and all 20-seed analyses run in minutes on a CPU. This modest budget is intentional: CARE-Judge is designed to be reproducible on a single commodity GPU plus metered API access.

\paragraph{Tie and invalid handling.}
An output that cannot be parsed as $A$/$B$ is retried once. If it is still unparseable we record it as an explicit \texttt{invalid} state rather than silently mapping it to the base verdict: an \texttt{invalid} perturbation call counts as a \emph{disagreement} with the base verdict, so it \emph{lowers} the corresponding stability signal and can only push an item toward abstention. We avoid mapping invalid$\to$base verdict, because that would inflate measured stability (an unparseable output would be counted as agreement). Genuine parse-invalid rates (the model returned an output we could not map to $A$/$B$) are below 3\% for every judge--benchmark pair. We distinguish these from \emph{transient API-service failures} (rate-limit/quota responses that return no model output): during GPT-5.5 collection a subset of calls failed this way, and because such failures reflect infrastructure rather than judge behavior we \emph{exclude} the affected items from GPT-5.5's analysis rather than count them as verdicts. The retained per-benchmark sample sizes for GPT-5.5 are JudgeBench 620, TL;DR 2{,}000, RewardBench 1{,}999, and LMArena 1{,}999 (Table~\ref{tab:main}); all other judges retain their full sets. The exclusions total two items across all four benchmarks ($1$ each on RewardBench and LMArena, $0.03\%$ of GPT-5.5's evaluations), so they cannot materially shift any reported statistic or introduce a selection bias: the transient rate-limit failures are triggered by request timing, not by item content, and the retained accuracy on each benchmark (Table~\ref{tab:main}) is within sampling noise of the full-set raw accuracy. We therefore treat the exclusion as missing-completely-at-random. For the preference benchmarks (TL;DR, LMArena) genuine ties in the human label are dropped before evaluation; JudgeBench and RewardBench carry objective correct/incorrect labels and contain no ties.

\paragraph{Rubric variants and the negative control (full detail).}
The $K{=}3$ rubric paraphrases used for the rubric-stability signal are reproduced verbatim in the released prompt file. They were written to hold the evaluation target (which response is better) fixed while varying wording. The negative-control experiment summarized in Section~\ref{sec:exp:main} used an \emph{intentionally non-equivalent} rubric set (one emphasizing brevity, one thoroughness, one formality) on both a mid-accuracy judge (DeepSeek-V4) and a near-random judge (Qwen2.5-1.5B), both on JudgeBench. The full per-condition counts are as follows. \textbf{DeepSeek-V4}: equivalent set flip rate 12.8\% (46/359) vs.\ non-equivalent 28.0\% (151/539), a $2.2\times$ ratio. \textbf{Qwen2.5-1.5B}: equivalent 31.0\% (189/610) vs.\ non-equivalent 16.7\% (102/610), i.e.\ the non-equivalent set was if anything \emph{more} stable. The mid-accuracy judge moves when the criterion changes (so equivalent-rubric flips are a conservative floor on real instability), whereas the near-random judge responds to surface features regardless of rubric content, consistent with a judge below the competence floor.

\section{Head-to-Head System Comparison (Full Table)}
\label{app:threeway}
Table~\ref{tab:threeway} reports the full three-way \emph{system} comparison summarized in Section~\ref{sec:exp:comparison}, at the matched six-call budget.

\begin{table}[h]
\centering
\small
\setlength{\tabcolsep}{3.5pt}
\caption{Three-way system comparison: held-out AUROC at the matched six-call budget (20 seeds, strict 40/30/30 split), all five judges. CARE is our fused calibrator; SCOPE is bidirectional-consistency confidence; ToE is simulated-annotator agreement at $N{=}0$ dedicated annotators. Best per row in \textbf{bold}. CARE is best on 14 of the 17 judge--benchmark pairs; the three exceptions are all on JudgeBench for a Qwen judge, where SCOPE's bidirectional score is marginally higher and the judge itself is near its accuracy floor.}
\label{tab:threeway}
\begin{tabular}{llrrr}
\toprule
Judge & Data & \textbf{CARE} & SCOPE & ToE \\
\midrule
Qwen2.5-1.5B & JB & 0.523 & \textbf{0.534} & 0.526 \\
Qwen2.5-1.5B & TL;DR & \textbf{0.583} & 0.570 & 0.525 \\
Qwen2.5-1.5B & RB & \textbf{0.713} & 0.425 & 0.492 \\
Qwen2.5-1.5B & Arena & \textbf{0.578} & 0.563 & 0.527 \\
\midrule
Qwen2.5-7B & JB & 0.548 & \textbf{0.578} & 0.508 \\
Qwen2.5-7B & TL;DR & \textbf{0.636} & 0.616 & 0.504 \\
Qwen2.5-7B & RB & \textbf{0.687} & 0.605 & 0.507 \\
Qwen2.5-7B & Arena & \textbf{0.599} & 0.588 & 0.508 \\
\midrule
DeepSeek-V4 & JB & \textbf{0.603} & 0.561 & 0.510 \\
DeepSeek-V4 & TL;DR & \textbf{0.675} & 0.616 & 0.519 \\
DeepSeek-V4 & RB & \textbf{0.831} & 0.606 & 0.513 \\
DeepSeek-V4 & Arena & \textbf{0.678} & 0.615 & 0.517 \\
\midrule
GPT-5.5 & JB & \textbf{0.834} & 0.695 & 0.526 \\
GPT-5.5 & TL;DR & \textbf{0.667} & 0.629 & 0.511 \\
GPT-5.5 & RB & \textbf{0.888} & 0.757 & 0.499 \\
GPT-5.5 & Arena & \textbf{0.645} & 0.571 & 0.510 \\
\midrule
Qwen2.5-14B & JB & 0.539 & \textbf{0.569} & 0.505 \\
\bottomrule
\end{tabular}
\end{table}

\section{Feature Definitions}
\label{app:features}
Table~\ref{tab:featdef} defines all 13 features of the vector $\boldsymbol\phi(x)$ in Eq.~\eqref{eq:features}. Confidence values are the judge's verbalized self-reported confidence in $[0,1]$; entropies are computed over the empirical winner distribution across the relevant call set and normalized to $[0,1]$.

\begin{table}[t]
\centering
\small
\setlength{\tabcolsep}{3pt}
\caption{The 13 features of $\boldsymbol\phi(x)$ (Eq.~\ref{eq:features}), grouped by family.}
\label{tab:featdef}
\begin{tabular}{p{0.30\columnwidth}p{0.60\columnwidth}}
\toprule
Feature & Definition \\
\midrule
\multicolumn{2}{l}{\emph{Protocol: position (swap)}}\\
swap\_consistency & $\mathbb{1}$[verdict unchanged after response reorder] (Eq.~\ref{eq:swap}).\\
swap\_conf\_gap & $|$conf$_{\text{orig}}-$conf$_{\text{swap}}|$: confidence change under reorder.\\
\midrule
\multicolumn{2}{l}{\emph{Protocol: rubric}}\\
rubric\_vote\_share & majority winner share over $K{=}3$ rubric paraphrases (Eq.~\ref{eq:rubric_share}).\\
rubric\_entropy & normalized entropy of the winner distribution over the $K$ rubrics.\\
rubric\_flip & $\mathbb{1}$[any rubric paraphrase changes the winner] (Eq.~\ref{eq:rubric_flip}).\\
\midrule
\multicolumn{2}{l}{\emph{Protocol: self-consistency}}\\
self\_vote\_share & majority winner share over $S{=}3$ samples (Eq.~\ref{eq:self}).\\
self\_entropy & normalized entropy of the winner distribution over the $S$ samples.\\
\midrule
\multicolumn{2}{l}{\emph{Confidence}}\\
confidence & aggregate verbalized confidence pooled across the perturbation calls.\\
base\_conf & verbalized confidence of the deterministic base verdict ($\tau{=}0$).\\
mean\_conf & mean verbalized confidence across all six calls.\\
std\_conf & standard deviation of verbalized confidence across the six calls.\\
\midrule
\multicolumn{2}{l}{\emph{Length}}\\
length\_gap & normalized token-length difference $|a_1|-|a_2|$ between the two responses.\\
score\_margin & difference between the base verdict's confidence and $0.5$ (decisiveness).\\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Per-seed risk reporting.}
For every model--dataset--$\alpha$ setting we release, per seed: the accepted count, coverage, realized test error $\hat R_{\text{test}}$, the Clopper--Pearson upper bound on the calibration split, and whether $\hat R_{\text{test}}>\alpha$. To characterize variance rather than only central tendency, we resample the 40/30/30 split over many random seeds, up to several hundred paired splits per setting where feasible, and report coverage and selective risk as mean$\pm$std across these splits alongside the pooled point estimates used in the main tables (pooling errors and accepts, rather than averaging per-seed ratios, so that zero-coverage seeds neither dominate nor are silently dropped). Aggregate seed robustness is high (coverage coefficient of variation $\approx2.4\%$ at $\alpha{=}0.15$). We also report the empirical risk-violation frequency $\Pr_{\text{seed}}(\hat R_{\text{test}}>\alpha)$ as our direct check on risk control; because the acceptance threshold is chosen data-dependently, this measured frequency is the quantity we validate against, and occasional violations at very low coverage (where only a handful of items are accepted) are expected and reported transparently.

\section{Exploratory: Cost-Aware Cascade}
\label{app:cascade}

This section uses the \textbf{same strict 40/30/30 split as the main experiments} and calibrates each tier \emph{conditionally} on the calibration items that actually reach it, with error budget $\delta_t=\delta/T$ (Section~\ref{sec:method:risk}). We still label it exploratory because it uses a single labeled pool per dataset and we have not stress-tested the conditional calibration under distribution shift.

We evaluate a three-tier cascade (Qwen-1.5B $\to$ DeepSeek-V4 $\to$ GPT-5.5), averaging over 10 seeds (Table~\ref{tab:cascade}). Only RewardBench yields net savings: the cheap tiers certify almost every item, giving 89\% paid-API savings at $\alpha{=}0.15$ (0.997 coverage, 0.908 accuracy) rising to 100\% at $\alpha{=}0.30$. On the other three benchmarks the cheap tiers certify little that survives the strict risk bound, so items either escalate to GPT-5.5 or are certified only after paying the earlier tiers, and savings are negative at $\alpha{=}0.15$ ($-11$ to $-14\%$); they turn positive only at the loose $\alpha{=}0.30$ (JudgeBench $+8\%$, TL;DR $+13\%$, LMArena $+54\%$), where the cheap tiers begin to accept at higher coverage. Savings are \emph{paid-API-token cost avoided on the accepted subset} relative to a single-call GPT-5.5 judge, and exclude local GPU time, throughput, and latency.

\begin{table}[t]
\centering
\small
\setlength{\tabcolsep}{4pt}
\caption{Cost-aware cascade (strict 40/30/30 split, conditional per-tier calibration, $\delta_t{=}\delta/T$, 10 seeds). Cov/Acc on held-out test; Save is paid-API-token cost avoided vs.\ single-call GPT-5.5.}
\label{tab:cascade}
\begin{tabular}{llrrr}
\toprule
Dataset & $\alpha$ & Cov.\ & Acc.\ & Save \\
\midrule
\multirow{2}{*}{JudgeBench} & 0.15 & 0.998 & 0.913 & $-$14\% \\
 & 0.30 & 0.998 & 0.881 & 8\% \\
\midrule
\multirow{2}{*}{TL;DR} & 0.15 & 0.087 & 0.752 & $-$14\% \\
 & 0.30 & 0.866 & 0.758 & 13\% \\
\midrule
\multirow{2}{*}{RewardBench} & 0.15 & 0.997 & 0.908 & 89\% \\
 & 0.30 & 0.999 & 0.768 & 100\% \\
\midrule
\multirow{2}{*}{LMArena} & 0.15 & 0.069 & 0.617 & $-$11\% \\
 & 0.30 & 0.821 & 0.748 & 54\% \\
\bottomrule
\end{tabular}
\end{table}

\section{Exploratory: Cross-Dataset Transfer}
\label{app:transfer}

We use the strict three-way split: the calibrator is fit on the \emph{source} dataset (train+cal) and evaluated on a held-out \emph{target} test split, averaged over 10 seeds (Table~\ref{tab:transfer}, GPT-5.5, $\alpha{=}0.15$). Two patterns emerge. Calibrators trained on \emph{objective} benchmarks (JudgeBench, RewardBench) transfer at near-full coverage ($0.98$--$1.00$) but with target-dependent accuracy, from $0.960$ (JudgeBench$\to$RewardBench, both objective) down to $0.707$ (JudgeBench$\to$LMArena). Calibrators trained on \emph{subjective} benchmarks transfer far more conservatively: an LMArena-trained calibrator accepts only $12.0\%$ of JudgeBench, $2.9\%$ of TL;DR, and $32.0\%$ of RewardBench, at accuracies of $0.60$--$0.80$. The open-ended-preference signal is thus the least portable, since the LMArena source yields the lowest coverage of any row, and transfer is most accurate when both source and target carry objective labels.

\begin{table}[t]
\centering
\small
\setlength{\tabcolsep}{4pt}
\caption{Cross-dataset transfer for GPT-5.5 ($\alpha{=}0.15$, strict 3-way split, source train+cal $\to$ target held-out test, 10 seeds). Subjective sources (LMArena, TL;DR) yield precise but low-coverage transfer; objective sources (JudgeBench, RewardBench) yield higher coverage at lower accuracy.}
\label{tab:transfer}
\begin{tabular}{lrr}
\toprule
Source $\to$ Target & Cov.\ & Acc.\ \\
\midrule
TL;DR $\to$ JudgeBench & 0.294 & 0.889 \\
TL;DR $\to$ RewardBench & 0.628 & 0.881 \\
TL;DR $\to$ LMArena & 0.277 & 0.730 \\
JudgeBench $\to$ TL;DR & 0.991 & 0.732 \\
JudgeBench $\to$ RewardBench & 0.999 & 0.960 \\
JudgeBench $\to$ LMArena & 0.991 & 0.707 \\
RewardBench $\to$ JudgeBench & 0.996 & 0.913 \\
RewardBench $\to$ TL;DR & 0.986 & 0.733 \\
RewardBench $\to$ LMArena & 0.985 & 0.709 \\
LMArena $\to$ JudgeBench & 0.120 & 0.596 \\
LMArena $\to$ TL;DR & 0.029 & 0.576 \\
LMArena $\to$ RewardBench & 0.320 & 0.798 \\
\bottomrule
\end{tabular}
\end{table}

\section{Risk--Coverage Curves}
\label{app:rcfig}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/risk_coverage.pdf}
\caption{Held-out risk--coverage curves (logistic fusion); lower selective risk at a given coverage is better, and the dashed line marks the $\alpha{=}0.15$ risk budget. Curves are shown per judge on JudgeBench and TL;DR.}
\label{fig:rc}
\end{figure}

\section{Signal-Level Accuracy Gaps}
\label{app:signals}

The per-benchmark accuracy gaps summarized by Figure~\ref{fig:gap} (in the main body) are tabulated below.

\begin{table}[t]
\centering
\small
\setlength{\tabcolsep}{4pt}
\caption{Signal-level accuracy on stable vs.\ unstable subsets, recomputed from the per-item feature traces (\texttt{scripts/compute\_signal\_gaps.py}) for all four primary judges. Gap $=$ stable $-$ unstable accuracy. Gaps are positive throughout for the mid-accuracy and frontier judges; the only non-positive entries are Qwen2.5-1.5B on RewardBench (rubric $-0.109$, position $-0.110$), the below-competence case where both subsets sit near chance and stability is uninformative (Section~\ref{sec:exp:stablewrong}).}
\label{tab:signals}
\begin{tabular}{lllrrr}
\toprule
Signal & Model & Dataset & Stable & Unstable & Gap \\
\midrule
\multirow{8}{*}{Rubric}
 & Qwen2.5-1.5B & JudgeBench & 0.511 & 0.459 & $+0.052$ \\
 & Qwen2.5-1.5B & RewardBench & 0.724 & 0.833 & $-0.109$ \\
 & DeepSeek-V4 & JudgeBench & 0.689 & 0.648 & $+0.041$ \\
 & DeepSeek-V4 & TL;DR & 0.708 & 0.555 & $+0.153$ \\
 & DeepSeek-V4 & RewardBench & 0.937 & 0.664 & $+0.274$ \\
 & DeepSeek-V4 & LMArena & 0.749 & 0.478 & $+0.271$ \\
 & Qwen2.5-7B & LMArena & 0.667 & 0.496 & $+0.171$ \\
 & GPT-5.5 & RewardBench & 0.962 & 0.692 & $+0.270$ \\
\midrule
\multirow{8}{*}{Position}
 & Qwen2.5-1.5B & JudgeBench & 0.553 & 0.469 & $+0.084$ \\
 & Qwen2.5-1.5B & RewardBench & 0.713 & 0.823 & $-0.110$ \\
 & DeepSeek-V4 & JudgeBench & 0.728 & 0.575 & $+0.153$ \\
 & DeepSeek-V4 & TL;DR & 0.746 & 0.518 & $+0.228$ \\
 & DeepSeek-V4 & LMArena & 0.772 & 0.528 & $+0.244$ \\
 & Qwen2.5-7B & TL;DR & 0.739 & 0.523 & $+0.216$ \\
 & GPT-5.5 & JudgeBench & 0.929 & 0.621 & $+0.308$ \\
 & GPT-5.5 & LMArena & 0.729 & 0.524 & $+0.205$ \\
\bottomrule
\end{tabular}
\end{table}

\section{Ensemble Baselines and Feature-Group Ablation}
\label{app:ensemble}

\begin{table}[t]
\centering
\small
\setlength{\tabcolsep}{3.2pt}
\caption{Ensemble baselines vs.\ CARE (held-out AUROC, 20 seeds), all four non-frontier/frontier judges. CARE is best or tied-best in 12 of 16 pairs; weighted vote is competitive for GPT-5.5 (it uses confidence implicitly) but has no risk-control mechanism.}
\label{tab:ensemble}
\begin{tabular}{llrrrr}
\toprule
Model & Data & \textbf{CARE} & SCOPE & MajVote & WtdVote \\
\midrule
\multirow{4}{*}{Qwen2.5-1.5B} & JB & 0.523 & \textbf{0.534} & 0.505 & 0.509 \\
 & TL;DR & \textbf{0.583} & 0.570 & 0.544 & 0.494 \\
 & RB & \textbf{0.713} & 0.425 & 0.538 & 0.620 \\
 & Arena & \textbf{0.578} & 0.563 & 0.559 & 0.494 \\
\midrule
\multirow{4}{*}{Qwen2.5-7B} & JB & 0.548 & \textbf{0.578} & 0.555 & 0.531 \\
 & TL;DR & \textbf{0.636} & 0.616 & 0.523 & 0.562 \\
 & RB & \textbf{0.687} & 0.605 & 0.602 & 0.624 \\
 & Arena & \textbf{0.599} & 0.588 & 0.569 & 0.531 \\
\midrule
\multirow{4}{*}{DeepSeek-V4} & JB & \textbf{0.603} & 0.561 & 0.585 & 0.581 \\
 & TL;DR & \textbf{0.675} & 0.616 & 0.609 & 0.618 \\
 & RB & \textbf{0.831} & 0.606 & 0.675 & 0.810 \\
 & Arena & \textbf{0.678} & 0.615 & 0.612 & 0.616 \\
\midrule
\multirow{4}{*}{GPT-5.5} & JB & \textbf{0.834} & 0.695 & 0.583 & 0.806 \\
 & TL;DR & 0.667 & 0.629 & 0.570 & \textbf{0.673} \\
 & RB & 0.888 & 0.757 & 0.600 & \textbf{0.899} \\
 & Arena & \textbf{0.645} & 0.571 & 0.568 & 0.643 \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[t]
\centering
\small
\setlength{\tabcolsep}{3.5pt}
\caption{Feature-group ablation (held-out AUROC, 20 seeds). ``FULL'' = all 13 features; each ablation uses only that group. Confidence dominates for GPT-5.5; protocol features carry the Qwen judges; length is near-chance everywhere.}
\label{tab:featgroup}
\begin{tabular}{llrrrr}
\toprule
Model & Data & \textbf{FULL} & protocol & conf & length \\
\midrule
\multirow{4}{*}{Qwen2.5-1.5B} & JB & 0.523 & \textbf{0.534} & 0.495 & 0.497 \\
 & TL;DR & \textbf{0.583} & 0.580 & 0.581 & 0.536 \\
 & RB & \textbf{0.713} & 0.659 & 0.631 & 0.609 \\
 & Arena & 0.578 & 0.579 & \textbf{0.587} & 0.535 \\
\midrule
\multirow{4}{*}{Qwen2.5-7B} & JB & 0.548 & \textbf{0.557} & 0.537 & 0.506 \\
 & TL;DR & \textbf{0.636} & 0.629 & 0.634 & 0.535 \\
 & RB & \textbf{0.687} & 0.615 & 0.685 & 0.493 \\
 & Arena & 0.599 & \textbf{0.603} & 0.587 & 0.520 \\
\midrule
\multirow{4}{*}{DeepSeek-V4} & JB & \textbf{0.603} & 0.586 & 0.623 & 0.509 \\
 & TL;DR & 0.675 & 0.660 & \textbf{0.683} & 0.548 \\
 & RB & \textbf{0.831} & 0.691 & 0.828 & 0.622 \\
 & Arena & \textbf{0.678} & 0.654 & 0.680 & 0.531 \\
\midrule
\multirow{4}{*}{GPT-5.5} & JB & 0.834 & 0.692 & \textbf{0.847} & 0.510 \\
 & TL;DR & 0.667 & 0.636 & \textbf{0.673} & 0.519 \\
 & RB & 0.888 & 0.782 & \textbf{0.906} & 0.669 \\
 & Arena & 0.645 & 0.598 & \textbf{0.647} & 0.505 \\
\bottomrule
\end{tabular}
\end{table}

\section{Length Bias by Judge and Benchmark}
\label{app:length}

Table~\ref{tab:lengthbias} reports how often each judge selects the \emph{longer} of the two responses, recovered by aligning the feature traces with the raw responses in \texttt{data/} (the \texttt{length\_gap} feature stores only the unsigned magnitude, so direction is recomputed here via \texttt{scripts/compute\_signal\_gaps.py}). Two patterns emerge. First, length preference is benchmark-driven: on the objectively-labeled JudgeBench and RewardBench every judge is near $50\%$ (no systematic length preference), whereas on the preference benchmarks TL;DR and LMArena all judges lean long, tracking the human label's own length correlation. Second, on LMArena the lean toward longer responses \emph{decreases} with judge accuracy ($67\%$ for Qwen2.5-1.5B down to $53\%$ for GPT-5.5), consistent with competence-gating: a stronger judge relies less on the length heuristic. This also explains why the length feature group is near-chance in the ablation (Table~\ref{tab:featgroup}).

\begin{table}[t]
\centering
\small
\setlength{\tabcolsep}{5pt}
\caption{Percentage of decisions selecting the \emph{longer} response (ties excluded), by judge and benchmark. Values near $50\%$ indicate no systematic length preference; ``--'' marks judge--benchmark pairs not run.}
\label{tab:lengthbias}
\begin{tabular}{lrrrr}
\toprule
Judge & JB & TL;DR & RB & Arena \\
\midrule
Qwen2.5-1.5B & 52 & 60 & 57 & 67 \\
DeepSeek-V4 & 46 & 59 & 48 & 66 \\
Qwen2.5-7B & 48 & 58 & 46 & 61 \\
GPT-5.5 & 44 & 58 & 43 & 53 \\
Qwen2.5-14B & 47 & -- & -- & -- \\
\bottomrule
\end{tabular}
\end{table}
