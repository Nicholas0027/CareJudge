% sections/conclusion.tex

\section{Conclusion}
\label{sec:conclusion}

We studied \emph{protocol stability} as a per-instance reliability signal for LLM-as-a-judge and found it to be \emph{competence-gated}. A verdict that changes under semantically equivalent rubrics, orderings, or resampling is unreliable---but only when the judge is accurate enough on the task that its instabilities track difficulty; below an accuracy threshold the signal inverts and a near-random judge is stably wrong. CARE-Judge implements this by fusing the three instability families into a learned calibrator with inherited selective-risk control~\citep{jung2025trust,badshah2026scope}, helping most in the mid-accuracy band, tying a saturated frontier judge, and inverting below the threshold.

\paragraph{Practical recommendation.}
Deploy protocol-stability abstention for mid-accuracy judges ($\approx0.65$--$0.8$ raw agreement), where fusion buys the largest risk-controlled coverage; the perturbation calls are optional for a frontier judge, and below the threshold one should abstain by default.

\paragraph{Limitations and Future Work.}
Risk control is empirical: the threshold is data-dependent and validated by measured violation rates, not a family-wise theorem, so a selective-conformal rule~\citep{xu2025selective,bao2024cap} would strengthen it. CARE needs task-specific labels and does not transfer freely (Appendix~\ref{app:transfer}), and stability never implies correctness. A crossed size$\times$recipe study, a guaranteed cascade, and pointwise scoring remain future work.
